"""
utils package -- shared modules for the Student Academic Risk Prediction
and Early Intervention System.

    model_utils.py     model loading + prediction helpers        (Stage 3)
    database.py        SQLite schema + CRUD                      (Stage 7)
    auth.py             authentication + password hashing         (Stage 8)
    validation.py        input validation                          (Stage 9)
    recommendations.py   intervention recommendation logic          (Stage 6)

Modules are added incrementally as each development stage is built --
nothing already in this package is renamed by a later stage.
"""
